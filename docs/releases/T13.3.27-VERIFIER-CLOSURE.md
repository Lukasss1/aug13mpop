# T13.3.27 — Deferred-POS verifier closure

T13.3.27 is a handoff-only correction. The application source, Supabase source and migration ledger are unchanged from T13.3.26.

It corrects two obsolete assertions that still expected a routed Till screen even though the public-web release intentionally defers POS:

- `scripts/pos-contract.test.mjs` now requires the retained ledger to stay role-scoped and `post_launch`, while proving it is not mounted in `AdminPanel`.
- `scripts/r48-till-boundary.test.mjs` now checks the current README truth: POS/Web Till source is retained for later integration and no Till route is exposed at launch.

This closes a real release-pipeline blocker because `npm run verify` executes both contracts. No POS function is added to the 14-function public deployment.
