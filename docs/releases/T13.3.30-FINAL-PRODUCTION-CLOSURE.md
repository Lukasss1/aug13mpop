# T13.3.30 — Final Production Closure

Release identity: `r4.10.15-t13.3.30-final-production-closure`.

This bounded patch reconciles the T13.3.29 deployment-hardening branches without changing MilkPop product behaviour or the database migration chain. It adds exact-incident protected recovery, shared production-input validation, one verified signed-dist materialiser, a code-owned public Edge Function inventory and signed backend-set fingerprint, skip-unchanged backend publication, predecessor-bound complete function recovery, release-1-only first-deploy handling and a manual-only protected production release trigger.

The v0.1 database remains 107 ordered migrations / 109 fresh-install SQL entries. The Edge Function source remains 17 directories: 14 public/staff functions are production-deployable and three POS functions remain deferred.

Production GO still requires protected locked CI, PostgreSQL 17 proof, real Supabase recovery/commissioning, owner/store/MFA bootstrap, full post-bootstrap backend acceptance and signed Netlify draft/live acceptance. Local source verification alone is not GO.
